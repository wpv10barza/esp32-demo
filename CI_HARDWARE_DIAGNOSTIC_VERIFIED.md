# CI verification marker

This branch exists only to trigger pull-request validation for the detailed Windows PnP + esptool hardware diagnostic, the exact Waveshare ESP32-S3R8 / 16 MB flash / 8 MB PSRAM runtime guard, the classic ESP32 compile-time rejection, and the real ESP32-S3 firmware build.
